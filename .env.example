# Environment Variables for Ceeps Website
# Copy this file to .env and fill in your actual values
# The .env file is gitignored and will NOT be committed to GitHub

# Admin password for Hall of Fame management
# Change this to a secure password in production!
ADMIN_PASSWORD=changeme

# Session secret for express-session
# Generate a random string for production (e.g., use: openssl rand -base64 32)
SESSION_SECRET=ceeps-hall-of-fame-secret-key-change-in-production

# Server port (Railway will set this automatically)
PORT=3000

# Railway volume mount path (Railway will set this automatically if using volumes)
# RAILWAY_VOLUME_MOUNT_PATH=
